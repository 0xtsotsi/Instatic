/**
 * Instatic-side: builds the nextActions[] handoff that tells the agent which
 * MCP tools to call next (site_apply_css, site_insert_html, …). Task 6 fills this in.
 */
export function buildNextActions(_payload: unknown): Array<{ tool: string; input: unknown }> {
  throw new Error('not implemented (Task 6)')
}
