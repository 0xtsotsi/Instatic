/**
 * Instatic-side: assigns `uid` attributes matching Instatic's 12-char id shape
 * (see existing page-tree nodes for the format). Task 5 will fill this in.
 */
export function assignUids(_html: string): { html: string; uids: string[] } {
  throw new Error('not implemented (Task 5)')
}
