/**
 * Rewrites a flat map of element-styles into class-scoped CSS rules.
 * PURE — no Instatic knowledge; the Instatic-side token rewriter lives in
 * adapters/tokens.ts. Task 3 will fill this in.
 */
export function rewriteCss(_styles: Record<string, Record<string, string>>): string {
  throw new Error('not implemented (Task 3)')
}
